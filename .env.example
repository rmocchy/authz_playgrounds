# Copy to .env for local docker compose (never commit .env).
# Placeholders only — change before any real use.

# --- Postgres (compose service: db) ---
POSTGRES_USER=playground
POSTGRES_PASSWORD=changeme
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# --- Auth (app authz platform — NOT an IdP) ---
AUTH_PORT=3001
AUTH_DATABASE_URL=postgres://playground:changeme@db:5432/auth

# --- Memo API ---
MEMO_PORT=3002
MEMO_DATABASE_URL=postgres://playground:changeme@db:5432/memo
# Base URL for Auth session verification (Memo → Auth)
AUTH_BASE_URL=http://auth:3001

# --- Web FE ---
WEB_PORT=5173

# --- Shared session cookie (Auth issues; browser sends to proxied APIs) ---
SESSION_COOKIE_NAME=playground_session

# Optional: seed user for local learning (implemented in later PRs)
# SEED_LOGIN_ID=demo
# SEED_PASSWORD=changeme
